namespace StarterKit.Domain.Enums;

/// <summary>
/// Defines the built-in role values used for seed data and authorization policy checks.
/// </summary>
public enum RoleType
{
    /// <summary>
    /// A user with full administrative privileges.
    /// </summary>
    Admin,

    /// <summary>
    /// A standard user with limited privileges.
    /// </summary>
    User
}
